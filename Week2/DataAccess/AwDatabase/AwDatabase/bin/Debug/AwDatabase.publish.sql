/*
Deployment script for vcdb

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "vcdb"
:setvar DefaultFilePrefix "vcdb"
:setvar DefaultDataPath ""
:setvar DefaultLogPath ""

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
IF EXISTS (SELECT 1
           FROM   [sys].[databases]
           WHERE  [name] = N'$(DatabaseName)')
    BEGIN
        ALTER DATABASE [$(DatabaseName)]
            SET ANSI_NULLS ON,
                ANSI_PADDING ON,
                ANSI_WARNINGS ON,
                ARITHABORT ON,
                CONCAT_NULL_YIELDS_NULL ON,
                QUOTED_IDENTIFIER ON,
                ANSI_NULL_DEFAULT ON 
            WITH ROLLBACK IMMEDIATE;
    END


GO
IF EXISTS (SELECT 1
           FROM   [sys].[databases]
           WHERE  [name] = N'$(DatabaseName)')
    BEGIN
        ALTER DATABASE [$(DatabaseName)]
            SET ALLOW_SNAPSHOT_ISOLATION OFF;
    END


GO
IF EXISTS (SELECT 1
           FROM   [sys].[databases]
           WHERE  [name] = N'$(DatabaseName)')
    BEGIN
        ALTER DATABASE [$(DatabaseName)]
            SET QUERY_STORE (QUERY_CAPTURE_MODE = ALL, CLEANUP_POLICY = (STALE_QUERY_THRESHOLD_DAYS = 367), MAX_STORAGE_SIZE_MB = 100) 
            WITH ROLLBACK IMMEDIATE;
    END


GO
IF EXISTS (SELECT 1
           FROM   [sys].[databases]
           WHERE  [name] = N'$(DatabaseName)')
    BEGIN
        ALTER DATABASE [$(DatabaseName)]
            SET QUERY_STORE = OFF 
            WITH ROLLBACK IMMEDIATE;
    END


GO
IF EXISTS (SELECT 1
           FROM   [sys].[databases]
           WHERE  [name] = N'$(DatabaseName)')
    BEGIN
        ALTER DATABASE SCOPED CONFIGURATION SET MAXDOP = 0;
    END


GO
PRINT N'Creating View [SalesLT].[vCustomerSales]...';


GO
CREATE VIEW [SalesLT].[vCustomerSales]
	AS 
Select c.CustomerID,c.CompanyName,
sum(soh.[TotalDue]) as Total,
min(soh.[TotalDue]) as MinOrder,
max(soh.[TotalDue]) as MaxOrder,
avg(soh.[TotalDue]) as AvgOrder
From [SalesLT].[Customer] c
Join [SalesLT].[SalesOrderHeader] soh
on c.CustomerID = soh.CustomerID
Group by c.CustomerID,c.CompanyName
GO
PRINT N'Creating View [SalesLT].[vCustomerSalesSummary]...';


GO
CREATE VIEW [SalesLT].[vCustomerSalesSummary]
	AS
Select c.CustomerID,c.CompanyName,
sum(soh.[TotalDue]) as Total,
min(soh.[TotalDue]) as MinOrder,
max(soh.[TotalDue]) as MaxOrder,
avg(soh.[TotalDue]) as AvgOrder
From [SalesLT].[Customer] c
Join [SalesLT].[SalesOrderHeader] soh
on c.CustomerID = soh.CustomerID
Group by c.CustomerID,c.CompanyName
GO
PRINT N'Update complete.';


GO
